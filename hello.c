#include "types.h"
#include "user.h"

int main(int argc, char *argv[]) {
  // for (int i = 0; i < 10; ++i) {
  //   int readcount = getreadcount();
  //   printf(1, "readcount: %d\n", readcount);
  //   int buf[1];
  //   read(0, &buf, 0);
  // }
  char *p = 0;
  printf(1, "%x\n", (unsigned int)*p);
  exit();
}
